#include "EnsaParameters.h"

EnsaParameters::EnsaParameters(int nsd, int nen, int nfaces, int numflx,
                               int numVars, int numEBC, int numNBC, int fType)
  : _nsd(nsd), _nen(nen), _nfaces(nfaces), _numflx(numflx), _numVars(numVars),
    _numEBC(numEBC), _numNBC(numNBC), _fType(fType)
{}
